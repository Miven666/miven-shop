<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="shop.content.mapper.PanelContentMapper">
    <resultMap id="BaseResultMap" type="shop.common.pojo.TbPanelContent">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="panel_id" jdbcType="INTEGER" property="panelId" />
        <result column="type" jdbcType="INTEGER" property="type" />
        <result column="product_id" jdbcType="BIGINT" property="productId" />
        <result column="sort_order" jdbcType="INTEGER" property="sortOrder" />
        <result column="full_url" jdbcType="VARCHAR" property="fullUrl" />
        <result column="pic_url" jdbcType="VARCHAR" property="picUrl" />
        <result column="pic_url2" jdbcType="VARCHAR" property="picUrl2" />
        <result column="pic_url3" jdbcType="VARCHAR" property="picUrl3" />
        <result column="created" jdbcType="TIMESTAMP" property="created" />
        <result column="updated" jdbcType="TIMESTAMP" property="updated" />
    </resultMap>

    <sql id="Example_Where_Clause">
        <where><foreach collection="orderCriteria" item="criteria" separator="or">
            <if test="criteria.valid"><trim prefix="(" prefixOverrides="and" suffix=")">
                <foreach collection="criteria.criteria" item="criterion">
                    <choose>
                        <when test="criterion.noValue">and ${criterion.condition}</when>
                        <when test="criterion.singleValue">and ${criterion.condition} #{criterion.value}</when>
                        <when test="criterion.betweenValue">and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}</when>
                        <when test="criterion.listValue">
                            and ${criterion.condition}
                            <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">#{listItem}</foreach>
                        </when>
                    </choose>
                </foreach>
            </trim></if>
        </foreach></where>
    </sql>

    <select id="selectByExample" parameterType="shop.common.pojo.TbPanelContentExample" resultMap="BaseResultMap">
        select
        <if test="distinct">distinct</if>
            id, panel_id, type, product_id, sort_order, full_url, pic_url, pic_url2, pic_url3, created, updated from tb_panel_content
        <if test="_parameter != null"><include refid="Example_Where_Clause" /></if>
        <if test="orderByClause != null">order by ${orderByClause}</if>
    </select>
</mapper>
