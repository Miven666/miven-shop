server:
  port: 8003

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8881/center/eureka/

spring:
  application:
    name: shop-content
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://sql.mz99.shop:3306/mshop?serverTimezone=GMT%2B8&useSSL=false&useUnicode=true&characterEncoding=utf-8
    username: root
    password: root123
  redis:
    port: 6379
    database: 1
    timeout: 10000
    host: cloud.mz99.shop
    jedis:
      pool:
        max-active: 100
        max-wait: -1
        max-idle: 20
        min-idle: 5

mybatis:
  mapper-locations: classpath:mapper/**/*.xml

panel:
  id: 0
  header: panel_header
